<!-- app/views/doses/_form.html.erb -->
<%= simple_form_for[@cocktail, @dose], do |f| %>
  <% if @dose.errors.any? %>
    <div class="errors-container">
      <ul>
        <% @dose.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.submit %>
<% end %>
